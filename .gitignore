
# ── Secrets & credentials (JANGAN pernah di-commit) ──
.env
.env.*
*.pem
*.key
*.p12
credentials.json
client_secret*.json
service-account*.json
config.local.php
GoogleService-Info.plist
# google-services.json  # buka komentar ini kalau mau Firebase config Android di-ignore

# ── Go (gateway-go) ──
bin/
*.exe
*.test
*.out
gateway-go/server

# ── Python (expert-py) ──
.venv/
__pycache__/
*.py[cod]
.pytest_cache/
*.egg-info/

# ── PHP backend ──
vendor/
# composer.lock SENGAJA tidak di-ignore (untuk aplikasi memang harus di-commit)

# ── Node / Next.js (web frontend) ──
node_modules/
.next/
out/
*.log
npm-debug.log*

# ── Android (Jetpack Compose / Gradle) ──
.gradle/
build/
local.properties
*.apk
*.aab
*.keystore
*.jks
captures/
.cxx/
*.iml

# ── iOS (Xcode / SwiftUI) ──
DerivedData/
*.xcuserstate
xcuserdata/
.build/
Pods/
*.ipa

# ── IDE / Editor ──
.idea/
.vscode/

# ── OS ──
.DS_Store
Thumbs.db

# ── Large binary / archive files ──
docs/*.zip
docs/*.tar.gz
docs/*.rar

# ── User-uploaded files ──
gateway-go/uploads/

*/.claude
/.claude
.claude/*
*/docs
docs/*
*.md
